# 安全策略 / Security Policy

## 怎么报告安全问题

**请不要开公开 issue。** 用下面任一渠道私下告诉我：

- GitHub 的 [Security Advisories](https://github.com/alchaincyf/huashu-gpt-image/security/advisories/new)（推荐，全程私密）
- 邮件：alchaincyf@gmail.com，标题带 `[SECURITY]`

我会在 **72 小时内**回一句确认收到，并在修复后于此文件的「已知问题」一节里记录。

如果你已经公开报告了，也没关系——这个仓库此前没有这份文件，没有渠道可循是我的问题，不是报告者的问题。

## 支持的版本

只维护 `master` 分支的最新提交。这是一个个人工具项目，没有长期支持分支。

## 已知问题

### `chatgpt-web-bridge` 本地桥无鉴权、且对任意来源开放 CORS

**状态：已确认，修复中。**

`chatgpt-web-bridge/server.py` 在 `127.0.0.1:8765` 上提供 `/health` `/poll` `/submit` `/result` 四个接口，
**没有任何鉴权**，并且对所有响应返回 `Access-Control-Allow-Origin: *`。

⚠️ **`server.py` 的注释和 README 里写的「只监听 127.0.0.1，不对外暴露」是一个误导性的说法。**
绑定回环地址挡不住浏览器发起的跨源请求：**你在浏览器里打开的任意一个网页**，
都可以 `fetch('http://127.0.0.1:8765/...')`，而那个 `*` 让它能读到响应体。

**在桥运行期间，一个恶意网页可以：**

- 通过 `/poll` 读走待处理任务，其中包含 `--ref` 参考图的完整 base64（也就是你还没发布的素材）
- 通过 `/result` 伪造生成结果，让 CLI 把攻击者提供的字节写进 `--out` 指定的文件
- 通过 `/submit` 投递任务，消耗你已登录账号的额度

**在修复发布之前，请这样降低风险：**

1. **不用的时候不要让 `server.py` 常驻。** 用完就 `Ctrl-C`，不要用 `&` 或 launchd 挂着。
2. 需要长时间开着时，尽量**不要在同一个浏览器里浏览不受信任的网页**。
3. 换端口（`PORT=9000`）**不构成防护**——端口范围是可以被枚举的。

修复方向：给桥加上一次性 token 鉴权，并把 CORS 从 `*` 收紧到扩展自己的 `chrome-extension://` 源。

## 这个项目不保护什么

说清楚边界，免得误报：

- **`scripts/` 下的脚本会调用你本机已登录的 ChatGPT / Codex 会话**，这是它的设计目的，不是漏洞。
- **本仓库不保存、不上传任何凭证。** 如果你发现了任何形式的外发端点，那是真问题，请按上面的渠道报告。
- 生成的图片和中间产物落在你自己指定的路径下，本项目不做清理。
